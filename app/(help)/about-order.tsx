import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const AboutOrder = (props: Props) => {
  return (
    <View>
      <Text>AboutOrder</Text>
    </View>
  );
};

export default AboutOrder;

const styles = StyleSheet.create({});
